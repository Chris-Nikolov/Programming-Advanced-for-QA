function solve(array) {

    for (let i = 0; i < arr.length; i++) {

        let personalNumber = arr[i].length;

        console.log(`Name: ${arr[i]} -- Personal Number: ${personalNumber}` );
    }
}

solve(
    [
        'Silas Butler',
        'Adnaan Buckley',
        'Juan Peterson',
        'Brendan Villarreal'
    ]
);